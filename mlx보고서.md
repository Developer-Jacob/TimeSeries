부제: MLX 기반 AI 모델을 활용한 STT 및 보이스피싱 탐지

작성일: 2025년 4월 8일

작성자: 임덕현

1. 프로젝트 개요
목적

iOS 기반 앱에서 실행 가능한 온디바이스 AI SDK를 개발하여,

실시간 음성 인식(STT)
보이스피싱 탐지

기능을 하나의 SDK로 통합 제공함으로써,

개인정보 보호, 실시간 반응성, 운영비용 절감이라는 3가지 핵심 목표 달성

주요 구성요소

ML 프레임워크: MLX (Apple Metal 기반 프레임워크)

언어 모델: Qwen (Alibaba LLM, Apache 2.0 오픈소스)

처리 구조: 100% 온디바이스 추론 (서버 호출 없음)

주요 기능: STT + 보이스피싱 탐지 + 실시간 경고 + 오프라인 작동

2. 기술 구성 및 채택 배경
MLX 도입 배경

MLX는 Apple이 발표한 Metal 기반 딥러닝 프레임워크로,

CPU, GPU를 적절히 배분하여 뛰어난 추론 속도와 에너지 효율

개발 방식은 PyTorch와 유사하여 학습 곡선이 낮고,

온디바이스 환경에 적합한 경량화된 모델 실행이 가능

기존 Pytorch 모델을 MLX 모델로 변환 가능

https://github.com/ml-explore

Qwen 모델 채택 이유 (향상된 모델로 전환 가능성 있음)

Qwen은 Alibaba의 고성능 LLM이며,

다국어 대응력 우수 (특히 한국어)
Apache 2.0 라이선스 → 상업적 활용 가능
다양한 모델 크기 (0.5B~7B) → 온디바이스 대응 가능
문맥 이해력이 뛰어나 STT 이후의 대화 분석에 효과적
Qwen2 0.5B instruct
https://huggingface.co/Qwen/Qwen2.5-0.5B-Instruct
3. 주요 기능 구성
STT (Speech-to-Text)
완전 온디바이스 처리 → 사용자 개인정보 보호
네트워크 연결 없이도 작동 가능 (오프라인 STT)
보이스피싱 탐지
LLM(Qwen)을 활용한 문맥 기반 위험 판단
단순 키워드 매칭이 아닌 정황/의도 분석
예: “지금 계좌번호 알려줘” → 실시간 경고 팝업 발생
온디바이스에서 실시간 작동 가능
4. 성능 벤치마크

통화 중 분석이라 실시간 성의 중요도는 낮다고 판단됨

보이스피싱의 경우 최대 3분까지 탐지 수행 (탐지에 충분한 시간으로 알려짐)

모델 크기: STT 150MB / Qwen 900MB (경량화 시도중)
메모리 점유율: 900MB ~ 1.3GB
iPhone 13 (RAM 4GB)
iPhone 14~15 (RAM 6GB)
iPhone 16 (RAM 8GB)
5. Architecture Flow

6. 데모 시나리오
상황:

사용자가 보이스 피싱으로 의심되는 통화를 진행함

SDK가 이를 감지하고 “보이스피싱 의심” 팝업을 즉시 띄움. or 경고 메시지 통화음 전송

(추후 개발)사용자는 녹음 또는 신고 버튼을 선택할 수 있음.

영상: https://confluence.tde.sktelecom.com/x/EpPbK
STT, VoicePhishing + 에이닷 전화
7. 개발 진행 상황 및 일정
현재 완료된 작업 (2025년 4월 기준):
MLX 기반 추론 엔진 구현
Qwen2 모델 온디바이스 통합
실시간 STT/탐지 기능 테스트 완료
에이닷 전화 연동
모델 다운로드(작업중)
향후 일정
앱 개발

모델 다운로드 기능

메모리, 배터리, 발열등 안정성 테스트

사내 테스트

서버 개발
모델 다운로드 OTA 개발(진행중)
모델 개발
모델 경량화 및 정확도 향상
8. LORA 적용 여부 및 판단 근거
LORA

LoRA(Low-Rank Adaptation) 최근 대형 언어 모델(LLM)에서 많이 활용되는 경량 파인튜닝 기법




기존에는 모델을 새롭게 학습하거나 재학습하려면 모든 파라미터(수천만~수억 개)를 업데이트해야 함

이 과정은 계산 비용도 크고, 메모리도 많이 소모되며, 특히 모바일·온디바이스 환경에서는 부담이 됨




하지만 LoRA는 이러한 문제를 해결하기 위해 고안된 기법으로,

모델의 핵심 부분에 아주 가벼운 “보조 학습 모듈”만 추가하고,

원래 모델은 그대로 두는 방식




쉽게 말해

기존 모델은 냉장고, LoRA는 탈부착 가능한 스마트 자석 메모판처럼 생각한다면

본체는 건드리지 않고, 메모판만 붙여서 원하는 내용을 바꾸는 방식




장점

학습 속도 빠름, 전체 모델이 아닌 일부 부위만 학습해도 충분함
모델변경시(다운로드 시) 저용량 Adapter만 변경 하면 됨
적용 여부

MLX 프레임워크와의 호환성 문제로 인해 이번 1차 버전에서는 LoRA 적용을 보류

LoRA는 모델 구조를 일부 “조립식”으로 바꾸는 기능이 필요하지만, MLX는 아직 완전하게 지원하지 않음

추후 MLX가 LoRA 또는 관련 기능을 정식 지원할 경우, LoRA 적용 여부를 재검토 예정


