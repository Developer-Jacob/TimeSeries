🔍 Preview 문제 정의
🔷 Preview build error

Preview 빌드 시 Inject되지 않는 객체들이 존재하여 View 초기화에 실패함

View 뿐만아니라 ViewModel, UseCase 등 디펜던시 있는 모든 feature들에 inject이 필요함

주입이 필요한 객체가 실제 앱 실행 시에는 제공되지만, Preview 환경에서는 누락되어 생기는 문제임.




🎯 시도 - preview inject helper ✅

이미 특정 Feature에서 해결책으로 preview inject helper를 만들어 사용하고 있음
공통화 필요
Preview 코드 내부에 Inject 로직 명시적으로 추가
https://gitlab.tde.sktelecom.com/AGENTIOS/Apollo-iOS/-/merge_requests/12854 에서 공통화 진행
🔷 ApolloNuguKit내, 외부 라이브러리에서 Preview 충돌 이슈 발생

🎯 시도 - Interfcae, Implementation 분리  

ApolloNuguKit을 Interface/Implementation구조로 분리하여 외부 모듈에서 Interface에만 의존하도록 시도함
의존성 방향을 외부 → Interface ← Implementation 형태로 구성하고자 했으나, Implementation와 Interface 간 강결합 존재

📌 특징

• 외부 SDK를 명확히 분리하고자 했으나, 기존 구조에서 Interface와 Implement 간 의존성이 높아 구조적 리팩터링 비용이 큼

• 장기적으로는 Interface와 구현을 느슨하게 연결하여 SDK 변경 가능성을 낮출 필요 있음




🎯 시도 - Data Layer 타겟 분리 ✅

기존 구조는 UI → Domain ← Data 형태의 계층 구조이나, 실제로는 모두 동일한 타겟(Implement)에 포함되어 있었음
이를 해결하기 위해 Data Layer를 별도의 SPM 타겟으로 분리함
UI/Domain에서는 Data에 직접 의존하지 않도록 구성하여 Preview와 테스트 환경에서 Mock 교체가 가능하도록 설계함

📌 특징

의존성 흐름은 유지하되, 물리적으로 Data 계층을 분리함으로써 Preview 문제 및 빌드 안정성 개선
Data 타겟 내에서만 외부 SDK를 참조하도록 제한하여, 전파되는 의존성을 최소화함




🔚 결론

Preview 환경에서의 안정성과 외부 SDK로부터의 영향도를 줄이기 위해 다음과 같은 조치가 필요함




✅ 1. Preview 전용 Inject 코드 작성

Preview 환경에서 Inject으로 사용할 객체를 명시적으로 주입

불완전한 의존성으로 인한 Preview 실패를 방지함




✅ 2. Data Layer의 명확한 타겟 분리

UI, Domain과 Data 계층을 Swift Package 기준으로 분리

Data에서만 외부 SDK에 접근하도록 하여, 상위 계층으로 의존성 전파를 차단함

ApolloNuguKit과 의존성 제거, Dependency 불가




✅ 3. 외부 SDK Interface/Implement 구조로 점진적 리팩터링

ApolloNuguKit처럼 강결합된 외부 모듈은 Interface 계층을 도입해 추상화

기존 구현체와 Interface 간의 연결을 느슨하게 만들어 변경 가능성과 테스트 유연성 확보




✅ 4. Stub/Mock 구조 정비

테스트 및 Preview 환경에서 실제 Data 의존 없이 실행 가능하도록

Data 계층 의존 객체를 Stub 또는 Mock으로 쉽게 교체 가능한 구조 유지
